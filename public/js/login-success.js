// Animación de login exitoso con círculos y mensaje personalizado

function animateLoginSuccess(username) {
  // Mostrar snackbar de login exitoso
  if (window.snackbarLoginSuccess) {
    window.snackbarLoginSuccess();
  }
}

